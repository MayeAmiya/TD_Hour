#pragma once

// ── Gadget / Widget Dimensions ──────────────────────────────────────────────

static constexpr int WIDGET_GADGET_SIZE = 16;

// Slider thumb dimensions
static constexpr int WIDGET_SLIDER_THUMB_WIDTH  = 13;
static constexpr int WIDGET_SLIDER_THUMB_HEIGHT = 16;
static constexpr int WIDGET_SLIDER_THUMB_POSITION = 10;  // THUMB_HEIGHT * 2/3

// Text padding
static constexpr int WIDGET_TEXT_PADDING = 4;

// Checkbox offset
static constexpr int WIDGET_CHECKBOX_OFFSET = 6;

// Cursor
static constexpr int WIDGET_CURSOR_THICKNESS = 2;

// Combobox
static constexpr int WIDGET_COMBO_ARROW_WIDTH = 20;
static constexpr int WIDGET_COMBO_ARROW_OFFSET = 6;

// Line height / dropdown
static constexpr int WIDGET_LINE_HEIGHT = 14;
static constexpr int WIDGET_COMBO_MAX_VISIBLE = 8;

// Character width estimate (approximate)
static constexpr int WIDGET_CHAR_WIDTH_ESTIMATE = 8;

// ── Slider ──────────────────────────────────────────────────────────────────

static constexpr int SLIDER_THUMB_WIDTH  = 13;
static constexpr int SLIDER_THUMB_HEIGHT = 16;
static constexpr int SLIDER_THUMB_POSITION = 10;
